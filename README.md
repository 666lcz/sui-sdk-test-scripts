# Sui SDK Test Scripts

This package serves as an minimal example of the usage of the @mysten/sui.js SDK.

To run the script,

```
npm i
node index.js
```

You can also use [npm link](https://docs.npmjs.com/cli/v8/commands/npm-link) or use a local dependency by changing `"@mysten/sui.js": "^0.11.0"` to `"@mysten/sui.js": "file:../sui/sdk/typescript"`
